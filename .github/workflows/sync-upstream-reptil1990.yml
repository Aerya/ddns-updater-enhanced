name: Sync upstream reptil1990/ddns-updater

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Clone reptil1990 upstream into temp dir
        run: |
          git clone --depth=1 https://github.com/reptil1990/ddns-updater.git /tmp/upstream-reptil1990

      - name: Sync files from upstream (excluding our own overrides)
        run: |
          rsync -av --delete \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='README.md' \
            /tmp/upstream-reptil1990/ ./

      - name: Patch Dockerfile labels to keep Aerya branding
        run: |
          sed -i \
            -e 's|org.opencontainers.image.authors="quentin.mcgaw@gmail.com"|org.opencontainers.image.authors="Aerya"|g' \
            -e 's|org.opencontainers.image.url="https://github.com/qdm12/ddns-updater"|org.opencontainers.image.url="https://github.com/Aerya/ddns-updater"|g' \
            -e 's|org.opencontainers.image.documentation="https://github.com/qdm12/ddns-updater"|org.opencontainers.image.documentation="https://github.com/Aerya/ddns-updater"|g' \
            -e 's|org.opencontainers.image.source="https://github.com/qdm12/ddns-updater"|org.opencontainers.image.source="https://github.com/Aerya/ddns-updater"|g' \
            -e 's|org.opencontainers.image.description="Universal DNS updater with WebUI"|org.opencontainers.image.description="Universal DNS updater with WebUI — based on qdm12/ddns-updater and reptil1990/ddns-updater"|g' \
            Dockerfile

      - name: Check for changes
        id: check
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Get upstream commit info
        if: steps.check.outputs.changed == 'true'
        id: upstream
        run: |
          SHA=$(git -C /tmp/upstream-reptil1990 rev-parse --short HEAD)
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"

      - name: Create sync branch and commit
        if: steps.check.outputs.changed == 'true'
        id: branch
        run: |
          BRANCH="sync/reptil1990-$(date +%Y%m%d-%H%M)"
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "chore: sync from reptil1990/ddns-updater@${{ steps.upstream.outputs.sha }} ($(date +%Y-%m-%d))"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"

      - name: Push branch
        if: steps.check.outputs.changed == 'true'
        run: git push origin "${{ steps.branch.outputs.branch }}"

      - name: Create PR and enable auto-merge
        if: steps.check.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="${{ steps.branch.outputs.branch }}"
          gh pr create \
            --title "chore: sync from reptil1990/ddns-updater $(date +%Y-%m-%d)" \
            --body "Automatic file sync from upstream [reptil1990/ddns-updater](https://github.com/reptil1990/ddns-updater) at commit \`${{ steps.upstream.outputs.sha }}\`.

          Files excluded from sync (local overrides kept):
          - \`.github/\` (our own workflows)
          - \`README.md\` (our own documentation)

          This PR will be merged automatically once all checks pass." \
            --base main \
            --head "$BRANCH"
          gh pr merge --squash --delete-branch --subject "chore: sync from reptil1990/ddns-updater $(date +%Y-%m-%d)" "$BRANCH" || true

      - name: No changes detected
        if: steps.check.outputs.changed == 'false'
        run: echo "No changes from reptil1990/ddns-updater. Nothing to do."
